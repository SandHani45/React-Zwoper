let screen = {
  width: window.screen.width,
  height: window.screen.height,
};
let style_rnd_thin_btn = {
  radius: 'round',
  height: 'thin',
  padding: '10px',
  btn_bck_Color: 'transparent',
  color: 'white',
  align: 'left',
  width: '100%',
  margin: '25px 0px 25px 0px',
};

let colors = {
  themeColor: '#59BE6C',
  white: '#fff',
  black: '#000',
};

let fonts = {
  size: {
    xs: '12px',
    sm: '14px',
    md: '18px',
    lg: '20px',
    x_lg: '22px',
  },

  weight: {
    xs_light: 300,
    light: 600,
    heavy: 800,
  },
};

export { colors, fonts, screen, style_rnd_thin_btn };
